/** Barrel export — see `docs/CRO-AUDIT.md` for exactly where each of these should be inserted (all insertions live in other agents' files, so none are wired in by this agent). */
export { ResponseTimeBadge } from './response-time-badge';
export { TrustRow } from './trust-row';
export { SeasonScarcity } from './season-scarcity';
export { SaveForLater } from './save-for-later';
