/** Barrel export — see `docs/ANALYTICS.md` for what each piece does and where it needs to be mounted. */
export { AnalyticsRoot } from './analytics-root';
export { ConsentSettingsLink } from './consent-settings-link';
